name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Check formatting
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "not gofmt'd:"
            echo "$unformatted"
            exit 1
          fi

      - name: Vet
        run: go vet ./...

      # The runner has Docker, but the default suite must not need it.
      # Pointing DOCKER_HOST at nothing makes any reach for a container
      # fail here rather than in a consumer's checkout.
      - name: Test
        env:
          DOCKER_HOST: unix:///nonexistent/docker.sock
        run: go test -race -count=1 ./...

  integration:
    name: Integration
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      # Build tags are additive, so this runs the default suite too. It
      # passes while no integration-tagged tests exist, which keeps the
      # job from blocking the tickets before the container-backed suite
      # lands.
      - name: Test
        run: go test -tags integration -race -count=1 ./...
